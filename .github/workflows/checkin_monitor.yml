name: Check-in Monitor

on:
  schedule:
    - cron: '*/10 2,3,4,5 * * 4,5'  # Wed/Thu 7PM-10PM PDT → Thu/Fri 02:00-05:00 UTC
  workflow_dispatch:

jobs:
  monitor-checkin:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      PLATFORM_URL: ${{ secrets.PLATFORM_URL }}
      PLATFORM_LOGIN_URL: ${{ secrets.PLATFORM_LOGIN_URL }}
      PLATFORM_EVENTS_URL: ${{ secrets.PLATFORM_EVENTS_URL }}
      PLATFORM_EMAIL: ${{ secrets.PLATFORM_EMAIL }}
      PLATFORM_PASSWORD: ${{ secrets.PLATFORM_PASSWORD }}
      EVENT_ORGANISER_NAME: ${{ secrets.EVENT_ORGANISER_NAME }}
      EVENT_LOCATION_WED: ${{ secrets.EVENT_LOCATION_WED }}
      EVENT_LOCATION_THU: ${{ secrets.EVENT_LOCATION_THU }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
      TWILIO_TO_NUMBER: ${{ secrets.TWILIO_TO_NUMBER }}
      SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
      SENDGRID_FROM_EMAIL: ${{ secrets.SENDGRID_FROM_EMAIL }}
      SENDGRID_TO_EMAIL: ${{ secrets.SENDGRID_TO_EMAIL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright chromium
        run: |
          playwright install chromium
          playwright install-deps chromium

      - name: Run check-in monitor
        run: python -m app.check_in

      - name: Report failure
        if: failure()
        run: |
          echo "❌ Check-in monitor failed — check logs above for details"
          echo "Common causes: login failed, selector changed, or missing secrets"
